# Name.com API Credentials
# Get these from your Name.com account: Account Settings > API Access

NAME_USERNAME=your-name-com-username
NAME_TOKEN=your-name-com-api-token

# API URL (defaults to test environment - this tool is experimental)
# NAME_API_URL=https://api.dev.name.com  # Test environment (default)
# NAME_API_URL=https://api.name.com      # Production environment 